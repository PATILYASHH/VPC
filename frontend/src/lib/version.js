// Single source of truth for VPC's three independent version tracks.
// Bump only the track that actually changed — see VERSIONING.md at repo root.
export const VPC_VERSION = '13.0.0';     // overall platform/release — installer, ecosystem-wide changes
export const VPC_OS_VERSION = '3.5.0';   // desktop shell — window manager, taskbar, launcher, app registry
export const VPCUI_VERSION = '1.0.0';    // shared UI/component layer used across all apps
